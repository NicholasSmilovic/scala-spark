# Open Learning Loops

Only unresolved ideas that should affect a future lesson belong here. Remove an item after the learner supplies closing evidence; retain the evidence in the relevant track summary.

| Area | Current evidence gap | Planned revisit | Closing evidence |
| --- | --- | --- | --- |
| Join correctness and cardinality | No demonstrated inner/left join contract, unmatched-reference behavior, or duplicate-key multiplication yet. | Curriculum 04, Milestones 1–3 | Tests exact matched/unmatched rows and duplicate-key multiplicity, then explains join cardinality. |
| Broadcast join decisions | Learner recognizes shuffle exchanges but has not compared shuffle-based and broadcast join physical plans or build-side memory tradeoffs. | Curriculum 04, Milestone 4 | Reads both formatted plans, identifies build/exchange behavior, and explains safe versus unsafe broadcast decisions. |

If the same gap recurs in a later curriculum, update the existing row rather than adding a duplicate.

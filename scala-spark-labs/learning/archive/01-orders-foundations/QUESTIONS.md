# Archived Questions — Curriculum 01

## Asked by me

- None recorded.

## Asked by the tutor

- None recorded. The first session has not started.

## Needs an experiment

- None for this milestone.

## Needs later platform context

- The learner connected `local[*]` to an interview and asked whether the whole company shares one “Scala cluster.” The relevant platform would be a Spark cluster or Spark compute platform, not a Scala cluster. The company's exact shared-versus-per-job topology is not established and belongs in a later deployment/platform project.
- The learner asked how a 500 GB dirty CSV or many CSV files would be validated, parallelized, and assembled into a data lake. The core partition/task model is relevant now; file sizing, data-lake layers, storage formats, and partition tuning belong in a later project.

## Answered

- Is Scala `main` a method or an entry point? It is both: `def main(...)` declares a method, and its recognized name and signature make it the application's entry point when launched.

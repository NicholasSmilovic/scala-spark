# Archived Current State — Curriculum 01

- Current milestone: Milestone 4 complete — the starter-project curriculum is complete.
- Current code: `src/main/scala/curriculum01/SparkOrders.scala` creates a local session, reads `data/curriculum-01/orders.csv` with header and schema inference, prints the schema, filters non-cancelled positive-quantity orders, adds `revenue = quantity * unit_price`, aggregates revenue and order count by region, sorts by total revenue descending, prints the formatted plan, displays the summary, and stops safely.
- Environment: The learner supplied `java -version` output verifying active 64-bit Temurin OpenJDK 21.0.11 LTS. The learner inspected the build files and verified that the project selects sbt 1.12.11, Scala 2.13.18, and Spark SQL 4.2.0. Scala is supplied by sbt; no global Scala installation is required.
- What has successfully run: Milestones 1–4 are complete. The final Java 21 run produced the required regional totals in descending order: East `770.0/2`, South `380.0/2`, West `340.0/2`, and North `240.0/2`. The formatted plan showed partial aggregation, a hash-partitioning exchange by region, final aggregation, a range-partitioning exchange by descending revenue, and sorting.
- Concepts demonstrated by the learner: Milestone 1 toolchain and Scala basics; `SparkSession`, `local[*]`, and safe cleanup; schema inference; `printSchema()` versus row output; lazy transformations and `show()` as an action; filtering and revenue enrichment; grouped aggregation; descending sorting; and identification of the grouping and sorting shuffle exchanges.
- Concepts introduced but not yet demonstrated: `col` predicates as Spark `Column` expressions; the return types/recomputation implications of multiple actions; and the precise distinction between schema inference, column pruning, and execution planning. The deeper meaning of Scala `Unit` remains deferred.
- Concepts intentionally deferred to a separate project: joins, broadcast decisions, caching, explicit partition tuning, Parquet, and external-system writes.
- Current blockers: None reported.
- Teaching preference confirmed by learner: The learner is most familiar with PHP/Laravel/npm and wants to build understanding by writing useful Scala after concrete context. Inspect saved files directly; do not ask for pasted source. Before edits, identify the exact file and location and ensure it is open. Keep a practical pace: bundle closely related edits into one coherent behavior change once the file and syntax pattern are established; do not baby the learner with token-sized steps or repeated readiness checks. Do not lead with pasted solutions, untaught terminology quizzes, manual row counting, abstract drills without context, or throwaway teaching lines.
- Recommended next lesson: Finish consolidating the observed schema-inference/column-pruning distinction, then stop this starter curriculum. Use a new project for later Spark topics.
- Last updated: 2026-07-31.

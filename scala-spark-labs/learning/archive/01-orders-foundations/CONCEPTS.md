# Archived Concepts — Curriculum 01

`Demonstrated` requires an accurate learner explanation, prediction, identification, or mostly independent fix. Code presence alone is not enough.

| Concept | Status | Evidence | My explanation | Next review |
| --- | --- | --- | --- | --- |
| Java, Scala, sbt, and Spark roles | Demonstrated | Toolchain versions verified. Learner accurately explained the compiler/JVM pipeline and distinguished Spark's data-processing role from sbt's build and dependency coordination role. | “sbt invokes the scala compiler to produce JVM bytecode, and JDK 21 supplies the JVM that executes it.” “Spark does the actual data [processing]… sbt just organizes the pieces.” | Milestone 1 |
| `SparkSession` | Demonstrated | Learner described `spark` as the application's session reference used for inspection/control and correctly identified `spark.version` as Spark's version rather than the sbt, Java/JVM, or Scala version. | “keeps reference to its spark instance” and “the Spark version… not the sbt… java… [or] scala version” | Milestone 1 |
| Local mode and `local[*]` | Demonstrated | Learner correctly identified `local[*]` as using all logical CPU cores available locally, rather than machines across a network. | “all available logical CPU” | Milestone 1 |
| Stopping Spark | Demonstrated | Learner explained that `spark.stop()` is still called after an error in the protected work because it is inside `finally`. | “spark.stop() will still be called because it's in the finally” | Milestone 1 |
| Scala `object` and `main` | Demonstrated | Learner explained the singleton `object`, recognized `main` as the Java-like application entry point, and correctly mapped `args: Array[String]` and the practical `Unit`/`void` comparison. Deeper `Unit` semantics are deferred until contrasting return types provide context. | “object… only one exists”; “it is like Java's main where it's the entry point”; `args: Array[String]`; “Unit is the closest thing” | Milestone 1 |
| Scala `val` | Demonstrated | Learner correctly predicted that reassignment would fail and then clearly distinguished the stable binding from possible mutation of the referenced object. | “spark isn't being reassigned. spark is mutable. the assignment is not.” | Milestone 1 |
| DataFrame | Introduced | Learner wrote and ran a header-aware, schema-inferred CSV reader pipeline. `printSchema()` showed seven named columns and `show()` displayed all twelve source rows; the learner's own explanation is still pending. | Pending | Milestone 2 |
| Schema inference | Demonstrated | Learner accurately explained that the default sampling ratio is 1.0 and therefore inference performs a full scan of column values to determine types. | “inferSchema defaults [to] 1.0 — full scan of column to determine type” | Milestone 2 |
| Action | Demonstrated | Learner corrected the earlier classification and explained that `show()` is the action that makes Spark work through the transformation plan. | “show is the action… action → now work through transformations” | Milestone 3 |
| Transformation | Demonstrated | Learner identified both `filter` and `withColumn` as transformations that extend the plan rather than immediately processing rows. | “filter/withColumn transformations” | Milestone 3 |
| Lazy evaluation | Demonstrated | Learner explained that assigning `validOrders` had not processed every row; it remained a plan until `show()` triggered execution. | “not yet… filter/withColumn is still just plan” | Milestone 3 |
| `filter` | Demonstrated | Learner wrote and ran both filters, verified the eight-row output, and identified them as lazy transformations. | “filter/withColumn transformations” | Milestone 3 |
| `withColumn` | Demonstrated | Learner wrote and ran the revenue expression, verified eight enriched rows, and independently calculated order `1010` revenue as `2 × 150.00 = 300.00`. | “300” for order `1010` | Milestone 3 |
| Spark `Column` expression | Introduced | Learner wrote and ran `=!=`, `>`, and multiplication expressions with `col`. Runtime behavior is correct; the learner still needs to explain that these construct Spark expressions rather than immediate Scala booleans/numbers. | Pending | Milestone 3 |
| `groupBy` and aggregation | Demonstrated | Learner built the correct regional totals/counts and explained Exchange `(5)` as “reshuffling regions, in order to sum.” | The first `HashAggregate` creates partial per-partition summaries; the hash exchange brings matching regions together; the second aggregate produces final regional values. | Future project |
| Sorting | Demonstrated | Learner sorted the four regional rows by descending total revenue and identified Exchange `(7)` as the reshuffle required for `orderBy`. | Global ordering uses range partitioning plus sorting; doing it after aggregation means this pipeline sorts the small regional result rather than all input orders. | Future project |
| Shuffle | Demonstrated | Learner identified both real exchanges and reasoned that reducing data before a required global order lowers shuffle volume. | Shuffles redistribute data between partitions. They are sometimes required for correctness; optimization means reducing unnecessary shuffled rows/columns and avoiding redundant wide operations, not minimizing partition count blindly. | Future project |
| `explain("formatted")` | Introduced | Learner printed and inspected the formatted physical plan, identifying both exchanges. Their description of it as finding “the optimal plan” needs the rule-/cost-based initial-plan and AQE nuance. | `explain` analyzes, optimizes, plans, and prints without processing the CSV rows. The printed adaptive plan may still change during execution. | Before next project |
